import styled from "styled-components";
import { RightContainer } from "./home";

const EventContainer = styled.div`
  display: flex;
  flex-direction: row;
`;


export {EventContainer, RightContainer};